targets:
  $default:
    builders:
      freezed:
        options:
          map_make_collections_unmodifiable: true
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
